[project]
name = "bacchus-evals"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "dspy>=3.0.4",
    "gepa>=0.0.17",
    "python-dotenv>=1.2.1",
]

[tool.hatch.build.targets.wheel]
packages = ["evaluators"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
